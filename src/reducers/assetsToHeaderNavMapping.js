const initialState = [
  {
    assetID: 'Beards',
    headerNavID: 'Beards'
  },
  {
    assetID: 'Hairstyles',
    headerNavID: 'Hairstyles'
  }
];

const reducer = (state = initialState, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default reducer;
